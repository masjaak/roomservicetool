# Handoff

## Project

- Repo: local Atelier Meridian Room Service workspace
- App: mobile-first room service web app for guests only
- iOS shell: Capacitor project in `ios/App`

## Current State

- Guest flow is still reducer/state-machine driven:
  `Login -> Menu -> Checkout -> Tracking -> Feedback`
- Firebase / Firestore integration is active in `src/lib/firebase.ts`
- EN / ID copy support remains in place
- iOS assets and shell are already wired for local simulator work
- Admin dashboard code has been removed from this repo and now lives separately

## Recent Cleanup

- Full test suite is green
- Production build is green
- Dead loading-theme files were removed:
  - `src/components/ScreenLoadingState.tsx`
  - `src/styles/guestTheme.ts`
- Old backup/generator artifacts were removed:
  - `ios-replacement/`
  - `ios/App.bak-20260422/`
  - root-level experimental Python scripts
- Vite alias noise from generator-style imports was removed
- `@capacitor/cli` is now a `devDependency`

## Generated Folders

- `build/` is generated by Vite and is ignored
- `ios/App/App/public/` is generated by `npx cap sync ios`
- Source web assets should be edited in `public/`, not in `ios/App/App/public/`

## Verification

- `npx vitest run` -> `20 passed`, `145 passed`
- `npm run build` -> success

## Main Files

- `src/App.tsx`
- `src/main.tsx`
- `src/views/LoginView.tsx`
- `src/views/MenuView.tsx`
- `src/views/CheckoutView.tsx`
- `src/views/TrackingView.tsx`
- `src/contexts/ThemeContext.tsx`
- `vite.config.ts`
- `capacitor.config.ts`

## Remaining Technical Debt

- `src/index.css` is still large and compiled-looking; it should eventually be broken into smaller maintainable style modules
- `App.tsx` still coordinates too much flow logic and can be split after UI settles
- No dedicated dependency-audit script exists yet, so future package cleanup is still manual

## Safe Next Steps

1. Run `npx cap sync ios` after any web build that should be reflected in Xcode
2. If simulator visuals change, verify both:
   - web source in `public/`
   - generated iOS copy in `ios/App/App/public/`
3. Continue UI work screen-by-screen without changing reducer behavior unless tests are updated first
